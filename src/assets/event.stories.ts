import type { Meta, StoryObj } from '@storybook/react';

import Event from '../event';
export default {
    title: 'Event',
    component: Event,
} as Meta <typeof Event>;

export const Primary: StoryObj<typeof Event> ={
};