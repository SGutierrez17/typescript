import type { Meta, StoryObj } from '@storybook/react';

import Context from '../context';

export default {
    title: 'Context',
    component: Context,
} as Meta <typeof Context>;

export const Primary: StoryObj<typeof Context> ={
}